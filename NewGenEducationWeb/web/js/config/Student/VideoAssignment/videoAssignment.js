
$(document).ready(function() {

	screenStepper=[{screenName:'videoAssignment',
                    // stepper for student profile
                    stepper:[
                    queryConfig
                           ] 
                      }];
	screenName="videoAssignment";
     createStepper('query',1,1,3);
     selectedOperation="query";
     
});