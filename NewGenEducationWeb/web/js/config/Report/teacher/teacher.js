
$(document).ready(function() {

	screenStepper=[{screenName:'teacherReport',
                    // stepper for student profile
                    stepper:[
                    queryConfig
                           ] 
                      }];
	screenName="teacherReport";
     createStepper('query',1,1,2);
     selectedOperation="query";
     
});